---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/pages/QuanLyKhoaHoc/components/Form.tsx",
  "src/pages/QuanLyKhoaHoc/index.tsx"
]
autonomous: true
requirements: ["UI-01", "UI-02", "FORM-01", "VAL-01"]
must_haves:
  truths:
    - "Người dùng thấy danh sách khóa học với đầy đủ các cột thông tin."
    - "Người dùng có thể mở form Thêm mới và Chỉnh sửa khóa học."
    - "Form hiển thị đầy đủ các trường dữ liệu và trình soạn thảo TinyEditor."
    - "Dữ liệu được lưu trữ và cập nhật vào localStorage thông qua model."
  artifacts:
    - path: "src/pages/QuanLyKhoaHoc/components/Form.tsx"
      provides: "Course Form component"
    - path: "src/pages/QuanLyKhoaHoc/index.tsx"
      provides: "Course List page"
  key_links:
    - from: "src/pages/QuanLyKhoaHoc/index.tsx"
      to: "src/pages/QuanLyKhoaHoc/components/Form.tsx"
      via: "TableBase Form prop"
    - from: "src/pages/QuanLyKhoaHoc/index.tsx"
      to: "useQuanLyKhoaHocModel"
      via: "useModel hook"

---

<objective>
Xây dựng giao diện trang Danh sách khóa học và Component Form Thêm/Sửa khóa học, tích hợp hoàn chỉnh với model và các component dùng chung (TableBase, TinyEditor).

Purpose: Cung cấp giao diện quản lý khóa học hoàn chỉnh cho người dùng.
Output: File `Form.tsx` và `index.tsx` trong thư mục `src/pages/QuanLyKhoaHoc`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/phase2/RESEARCH.md
@src/components/Table/index.tsx
@src/components/TinyEditor/index.tsx
@src/models/useQuanLyKhoaHocModel.ts
@src/services/QuanLyKhoaHoc/typing.d.ts
@src/services/QuanLyKhoaHoc/constant.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Xây dựng Component Form khóa học</name>
  <files>src/pages/QuanLyKhoaHoc/components/Form.tsx</files>
  <action>
    Tạo component `FormKhoaHoc` sử dụng Ant Design `Form` và tích hợp `useQuanLyKhoaHocModel`.
    - Sử dụng `useModel('useQuanLyKhoaHocModel')` để lấy các state: `record`, `edit`, `setVisibleForm`, `postModel`, `putModel`, `loading`.
    - Triển khai các trường dữ liệu (per D-01):
      - `tenKhoaHoc`: Input, validation: required (Bắt buộc nhập tên khóa học), max 100 characters.
      - `idGiangVien`: Select, lấy options từ `DANH_SACH_GIANG_VIEN`. Validation: required.
      - `soLuongHocVien`: InputNumber, min 0. Validation: required.
      - `trangThai`: Select, lấy options từ `TRANG_THAI_LABEL` (D-02). Validation: required.
      - `moTa`: Tích hợp `TinyEditor` component.
    - Xử lý logic Add/Edit:
      - `onFinish`: Gọi `putModel(record._id, values)` nếu `edit === true`, ngược lại gọi `postModel(values)`.
      - Sử dụng `form.setFieldsValue(record)` khi `record` thay đổi hoặc modal mở để fill dữ liệu khi sửa.
    - UI: Bao gồm footer có nút "Lưu" (submit) và "Hủy" (setVisibleForm(false)).
  </action>
  <verify>
    Thực hiện kiểm tra component:
    1. Kiểm tra các trường hiển thị đầy đủ: Tên, Giảng viên, Số lượng, Trạng thái, Mô tả (TinyEditor).
    2. Kiểm tra validation: Báo lỗi khi để trống hoặc nhập tên > 100 ký tự.
    3. Kiểm tra logic Edit: Form hiển thị đúng dữ liệu cũ khi model ở trạng thái edit.
  </verify>
  <done>Component Form.tsx hoàn thiện, đúng validation và xử lý được cả Thêm/Sửa.</done>
</task>

<task type="auto">
  <name>Task 2: Xây dựng Trang Danh sách khóa học</name>
  <files>src/pages/QuanLyKhoaHoc/index.tsx</files>
  <action>
    Tạo component `QuanLyKhoaHocPage` sử dụng `TableBase` để hiển thị danh sách khóa học.
    - Cấu hình `TableBase` với các props:
      - `modelName`: 'useQuanLyKhoaHocModel'
      - `title`: 'Quản lý khóa học'
      - `Form`: `FormKhoaHoc` (import từ `./components/Form`)
      - `columns`: Định nghĩa mảng cột theo yêu cầu REQ-01:
        - TT: `addStt={true}`.
        - Tên khóa học: `dataIndex: 'tenKhoaHoc'`, `filterType: 'string'`, `sortable: true`.
        - Giảng viên: `dataIndex: 'idGiangVien'`, render tên giảng viên bằng cách tìm trong `DANH_SACH_GIANG_VIEN`.
        - Số lượng học viên: `dataIndex: 'soLuongHocVien'`, `sortable: true`.
        - Trạng thái: `dataIndex: 'trangThai'`, render label từ `TRANG_THAI_LABEL`.
        - Thao tác: Cột chứa 2 nút Edit (gọi `handleEdit(record)`) và Delete (gọi `deleteModel(record._id)` với `Popconfirm`).
    - Đảm bảo `TableBase` tích hợp đúng với model để xử lý phân trang, tìm kiếm và đóng/mở form.
  </action>
  <verify>
    Truy cập trang Quản lý khóa học (cần giả lập route nếu chưa có):
    1. Danh sách hiển thị đúng dữ liệu mẫu từ localStorage.
    2. Nút "Thêm mới" mở ra Form trống.
    3. Nút "Sửa" mở ra Form có dữ liệu của bản ghi tương ứng.
    4. Nút "Xóa" hiển thị confirm và xóa thành công nếu số lượng học viên = 0.
  </verify>
  <done>Trang index.tsx hoàn thiện, tích hợp đầy đủ TableBase, Form và Model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> UI | Input validation for course data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Course Form | mitigate | Client-side validation for required fields and character limits |
| T-02-02 | Information Disclosure | TinyEditor | mitigate | Ensure TinyEditor only loads trusted local/standard configuration |
</threat_model>

<verification>
- Chạy `npm start` và kiểm tra trực tiếp giao diện.
- Kiểm tra tính năng Tìm kiếm theo tên trên bảng.
- Kiểm tra tính năng Sắp xếp theo số lượng học viên.
- Kiểm tra flow Thêm -> Sửa -> Xóa một khóa học.
</verification>

<success_criteria>
- File `src/pages/QuanLyKhoaHoc/components/Form.tsx` tồn tại và hoạt động đúng logic.
- File `src/pages/QuanLyKhoaHoc/index.tsx` tồn tại và hiển thị đúng dữ liệu qua `TableBase`.
- Các yêu cầu về validation (max 100 ký tự, required) được đáp ứng.
- Trình soạn thảo `TinyEditor` hiển thị và lưu dữ liệu mô tả (HTML) chính xác.
</success_criteria>

<output>
After completion, create `.planning/phases/phase2/02-01-SUMMARY.md`
</output>
