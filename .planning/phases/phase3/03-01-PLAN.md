---
phase: "03"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["src/pages/QuanLyKhoaHoc/DanhSach.tsx", "src/pages/QuanLyKhoaHoc/ThongKe.tsx", "config/routes.ts", "src/locales/vi-VN/menu.ts"]
autonomous: true
requirements: ["MENU-01", "STAT-01", "STAT-02"]
must_haves:
  truths:
    - "Menu 'Quản lý khóa học' hiển thị 2 submenu: 'Danh sách' và 'Thống kê'"
    - "Truy cập /quan-ly-khoa-hoc/danh-sach hiển thị bảng danh sách khóa học"
    - "Truy cập /quan-ly-khoa-hoc/thong-ke hiển thị các biểu đồ thống kê"
    - "Biểu đồ Donut hiển thị tỉ lệ trạng thái khóa học"
    - "Biểu đồ Cột hiển thị số lượng học viên theo từng khóa học"
  artifacts:
    - path: "src/pages/QuanLyKhoaHoc/DanhSach.tsx"
      provides: "Giao diện danh sách khóa học"
    - path: "src/pages/QuanLyKhoaHoc/ThongKe.tsx"
      provides: "Giao diện thống kê biểu đồ"
    - path: "config/routes.ts"
      provides: "Cấu hình route cho submenu"
  key_links:
    - from: "config/routes.ts"
      to: "src/pages/QuanLyKhoaHoc/DanhSach.tsx"
      via: "component path"
    - from: "src/pages/QuanLyKhoaHoc/ThongKe.tsx"
      to: "src/models/useQuanLyKhoaHocModel.ts"
      via: "useModel hook"
---

<objective>
Tái cấu trúc menu Quản lý khóa học thành submenu và bổ sung trang Thống kê với biểu đồ (Charts).

Mục tiêu:
- Cải thiện trải nghiệm điều hướng bằng cách phân chia rõ ràng giữa quản lý dữ liệu và xem báo cáo.
- Cung cấp cái nhìn tổng quan về dữ liệu khóa học thông qua biểu đồ trực quan.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/phase3/RESEARCH.md
@src/models/useQuanLyKhoaHocModel.ts
@src/components/Chart/DonutChart.tsx
@src/components/Chart/ColumnChart.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cấu trúc lại Menu và Route</name>
  <files>src/pages/QuanLyKhoaHoc/DanhSach.tsx, config/routes.ts, src/locales/vi-VN/menu.ts</files>
  <action>
    - Đổi tên (rename) file `src/pages/QuanLyKhoaHoc/index.tsx` thành `src/pages/QuanLyKhoaHoc/DanhSach.tsx`. 
    - Cập nhật `config/routes.ts`: thay đổi route `QuanLyKhoaHoc` hiện tại thành cấu trúc có `routes` con bao gồm 'Danh sách' (path: 'danh-sach') và 'Thống kê' (path: 'thong-ke'). Thêm redirect từ `/quan-ly-khoa-hoc` về `/quan-ly-khoa-hoc/danh-sach`.
    - Cập nhật `src/locales/vi-VN/menu.ts`: Thêm các key translation:
      - 'menu.QuanLyKhoaHoc': 'Quản lý khóa học'
      - 'menu.QuanLyKhoaHoc.DanhSach': 'Danh sách'
      - 'menu.QuanLyKhoaHoc.ThongKe': 'Thống kê'
  </action>
  <verify>
    <automated>ls src/pages/QuanLyKhoaHoc/DanhSach.tsx && grep -q "QuanLyKhoaHoc.DanhSach" config/routes.ts</automated>
  </verify>
  <done>File đã được đổi tên, routes đã được cập nhật đúng cấu trúc submenu và locale đã được bổ sung.</done>
</task>

<task type="auto">
  <name>Task 2: Triển khai trang Thống kê biểu đồ</name>
  <files>src/pages/QuanLyKhoaHoc/ThongKe.tsx</files>
  <action>
    - Tạo file `src/pages/QuanLyKhoaHoc/ThongKe.tsx`.
    - Sử dụng `useModel('useQuanLyKhoaHocModel')` để lấy dữ liệu khóa học (gọi `getModel({}, 1, 1000)` để đảm bảo lấy đủ dữ liệu cho thống kê).
    - Tính toán dữ liệu cho 2 biểu đồ:
      1. **DonutChart (Trạng thái):** Đếm số lượng khóa học theo từng trạng thái (Đang mở, Đã kết thúc, Tạm dừng).
      2. **ColumnChart (Học viên):** Hiển thị số lượng học viên cho từng khóa học (xAxis: tên khóa học, yAxis: số lượng học viên).
    - Sử dụng `Row`, `Col`, `Card` từ `antd` để bố cục trang đẹp mắt.
  </action>
  <verify>
    <automated>grep -q "DonutChart" src/pages/QuanLyKhoaHoc/ThongKe.tsx && grep -q "ColumnChart" src/pages/QuanLyKhoaHoc/ThongKe.tsx</automated>
  </verify>
  <done>Trang Thống kê được tạo với đầy đủ 2 loại biểu đồ hiển thị dữ liệu thực tế từ model.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Hệ thống Menu submenu và Trang Thống kê với biểu đồ.</what-built>
  <how-to-verify>
    1. Chạy ứng dụng và kiểm tra menu bên trái.
    2. Click vào 'Quản lý khóa học' để xem 2 menu con 'Danh sách' và 'Thống kê'.
    3. Kiểm tra trang 'Danh sách' vẫn hoạt động bình thường sau khi đổi tên file.
    4. Kiểm tra trang 'Thống kê' xem các biểu đồ có hiển thị đúng dữ liệu (thêm/sửa dữ liệu ở trang Danh sách và xem sự thay đổi ở Thống kê).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → LocalStorage | Dữ liệu thống kê được đọc trực tiếp từ LocalStorage qua model. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | LocalStorage | accept | Dữ liệu mô phỏng, không chứa thông tin nhạy cảm. |
| T-03-02 | Tampering | LocalStorage | accept | Client-side only, người dùng có thể sửa LocalStorage của chính họ. |
| T-03-03 | Denial of Service | Chart Rendering | accept | Số lượng khóa học nhỏ, không gây quá tải cho việc render biểu đồ. |
</threat_model>

<success_criteria>
- Cấu trúc menu phân cấp hoạt động tốt trong `config/routes.ts`.
- Trang Thống kê hiển thị dữ liệu trực quan bằng `ApexCharts`.
- Không làm gãy tính năng CRUD hiện có sau khi refactor file.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/phase3/03-01-SUMMARY.md`
</output>
