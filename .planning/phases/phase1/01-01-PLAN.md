---
phase: 01-setup-types-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/services/QuanLyKhoaHoc/typing.d.ts, src/services/QuanLyKhoaHoc/constant.ts]
autonomous: true
requirements: [PHASE-1]

must_haves:
  truths:
    - "Dữ liệu mẫu giảng viên khả dụng để sử dụng trong dropdown"
    - "Định nghĩa kiểu KhoaHoc.Record bao gồm đầy đủ các trường yêu cầu"
  artifacts:
    - path: "src/services/QuanLyKhoaHoc/typing.d.ts"
      provides: "Namespace KhoaHoc and IRecord interface"
    - path: "src/services/QuanLyKhoaHoc/constant.ts"
      provides: "Mock data for teachers and Status enum"
---

<objective>
Thiết lập các kiểu dữ liệu (Types) và hằng số (Constants) cho tính năng Quản lý khóa học.
Đây là bước nền tảng để đảm bảo tính nhất quán của dữ liệu trong toàn bộ ứng dụng.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@src/services/ThongBao/typing.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Định nghĩa kiểu dữ liệu KhoaHoc</name>
  <files>src/services/QuanLyKhoaHoc/typing.d.ts</files>
  <action>
    Tạo file `src/services/QuanLyKhoaHoc/typing.d.ts` và định nghĩa namespace `KhoaHoc`.
    Cần định nghĩa:
    - `IRecord`:
      - `_id`: string (unique)
      - `tenKhoaHoc`: string (max 100 ký tự)
      - `idGiangVien`: string
      - `soLuongHocVien`: number
      - `trangThai`: ETrangThai (Đang mở, Đã kết thúc, Tạm dừng)
      - `moTa`: string (HTML string)
    - `IGiangVien`:
      - `_id`: string
      - `hoTen`: string
  </action>
  <verify>
    Kiểm tra file tồn tại và không có lỗi cú pháp TypeScript.
    `npx tsc src/services/QuanLyKhoaHoc/typing.d.ts --noEmit`
  </verify>
  <done>File typing.d.ts được tạo với đầy đủ các interface yêu cầu.</done>
</task>

<task type="auto">
  <name>Task 2: Khởi tạo dữ liệu mẫu và Enum trạng thái</name>
  <files>src/services/QuanLyKhoaHoc/constant.ts</files>
  <action>
    Tạo file `src/services/QuanLyKhoaHoc/constant.ts`.
    Định nghĩa:
    - `enum ETrangThai`: 'DANG_MO', 'DA_KET_THUC', 'TAM_DUNG'.
    - `const DANH_SACH_GIANG_VIEN`: Mảng chứa ít nhất 3 giảng viên mẫu (VD: Nguyễn Văn A, Trần Thị B, Lê Văn C).
    - `const TRANG_THAI_LABEL`: Map enum sang nhãn hiển thị tiếng Việt.
  </action>
  <verify>
    Kiểm tra file tồn tại và export đúng các hằng số.
    Sử dụng lệnh `cat` để xem nội dung file.
  </verify>
  <done>Hằng số và dữ liệu mẫu giảng viên sẵn sàng sử dụng.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Internal Types | Chỉ sử dụng trong dự án, không có rủi ro bảo mật trực tiếp |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | typing.d.ts | accept | Không chứa thông tin nhạy cảm |
</threat_model>

<success_criteria>
- File `typing.d.ts` định nghĩa đúng cấu trúc `KhoaHoc.IRecord`.
- File `constant.ts` cung cấp dữ liệu mẫu cho giảng viên.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/01-setup-types-model/01-01-SUMMARY.md`
</output>
