---
phase: 01-setup-types-model
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/models/useQuanLyKhoaHocModel.ts]
autonomous: true
requirements: [PHASE-1]

must_haves:
  truths:
    - "Dữ liệu được lưu trữ và truy xuất từ localStorage"
    - "Thao tác CRUD hoạt động chính xác (Thêm, Sửa, Xóa, Lấy danh sách)"
    - "Validation tên khóa học không trùng lặp hoạt động"
    - "Điều kiện xóa khóa học (số lượng học viên = 0) được áp dụng"
  artifacts:
    - path: "src/models/useQuanLyKhoaHocModel.ts"
      provides: "Custom model hook with CRUD methods"
---

<objective>
Xây dựng custom model `useQuanLyKhoaHocModel.ts` sử dụng Generics và `localStorage` để quản lý dữ liệu khóa học.
Model này sẽ mô phỏng các phương thức API chuẩn để có thể dễ dàng thay thế bằng API thật trong tương lai.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@src/hooks/useInitModel.tsx
@src/services/QuanLyKhoaHoc/typing.d.ts
@src/services/QuanLyKhoaHoc/constant.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Xây dựng cấu trúc cơ bản của model</name>
  <files>src/models/useQuanLyKhoaHocModel.ts</files>
  <action>
    Khởi tạo hook `useQuanLyKhoaHocModel` kế thừa tư tưởng từ `useInitModel`.
    Cần định nghĩa các state:
    - `danhSach`: KhoaHoc.IRecord[]
    - `loading`: boolean
    - `total`: number
    - `visibleForm`: boolean
    - `edit`: boolean
    - `record`: KhoaHoc.IRecord | undefined
    Sử dụng key `QUAN_LY_KHOA_HOC_DATA` trong `localStorage`.
  </action>
  <verify>
    Kiểm tra file có export default function.
  </verify>
  <done>Hook được khởi tạo với đầy đủ state cần thiết.</done>
</task>

<task type="auto">
  <name>Task 2: Triển khai các phương thức CRUD với localStorage</name>
  <files>src/models/useQuanLyKhoaHocModel.ts</files>
  <action>
    Triển khai các hàm sau:
    - `getModel()`: Đọc từ `localStorage`, hỗ trợ tìm kiếm theo tên, trả về `danhSach`.
    - `postModel(payload)`:
      - Kiểm tra trùng tên khóa học (`tenKhoaHoc`).
      - Tạo `_id` mới (UUID hoặc timestamp).
      - Lưu vào `localStorage`.
    - `putModel(id, payload)`:
      - Kiểm tra trùng tên khóa học (trừ record đang sửa).
      - Cập nhật record theo `id`.
    - `deleteModel(id)`:
      - Kiểm tra `soLuongHocVien === 0`.
      - Xóa record và cập nhật `localStorage`.
    Lưu ý: Sử dụng `message.success` và `message.error` từ `antd` cho phản hồi người dùng.
  </action>
  <verify>
    Viết script kiểm tra nhanh CRUD (hoặc visual check qua Console browser sau này).
    Ở giai đoạn này, kiểm tra logic hàm bằng cách đọc mã nguồn.
  </verify>
  <done>Toàn bộ logic CRUD đã hoàn thành và sẵn sàng cho UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Local Storage | Dữ liệu được lưu ở phía client, có thể bị chỉnh sửa bởi người dùng |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | localStorage | accept | Không lưu thông tin bảo mật, chỉ là dữ liệu khóa học mẫu |
| T-01-03 | Tampering | CRUD Logic | mitigate | Kiểm tra tính hợp lệ của dữ liệu đầu vào (Validation) |
</threat_model>

<success_criteria>
- Hook export các hàm: `getModel`, `postModel`, `putModel`, `deleteModel`.
- Các hàm thực hiện đúng logic nghiệp vụ và validation.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/01-setup-types-model/01-02-SUMMARY.md`
</output>
